
---

### `04-plantio/grass.md`

```markdown
# Grass — A grama da fazenda

A `Grass` possui uma característica diferente das outras plantas que
estamos estudando.

Ela cresce automaticamente quando o terreno está como `Grassland`.

Isso significa que não precisamos utilizar:

```python
plant(Entities.Grass)

# Verifica se a grama está pronta
if can_harvest():

    # Colhe a grama
    harvest()
